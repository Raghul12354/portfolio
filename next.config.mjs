/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio",
    reactStrictMode: true,
    images: {
        domains: ['skillicons.dev']
    }
};

export default nextConfig;
