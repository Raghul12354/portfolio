/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio",
    images: {
        domains: ['skillicons.dev'],
        unoptimized: true,
    }
};

export default nextConfig;
